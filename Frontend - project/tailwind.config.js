/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Include all source files
    ],
    darkMode: 'class', // Enable 'class' or 'media' for dark mode
    theme: {
      extend: {}, // Customize your theme here
    },
    plugins: [], // Add any Tailwind plugins here
  };
  